import { withInstall } from '@element-plus/utils/with-install'
import { ConfigProvider } from './config-provider'

export const LyConfigProvider = withInstall(ConfigProvider)
export default LyConfigProvider
